import lookAction from './look';
import examineAction from './examine';
import searchAction from './search';
import pickupAction from './pickup';

export const actions = [lookAction(), examineAction(), searchAction(), pickupAction()];
